import { Routes } from '@angular/router';

import { ClientComponent } from './components/client/client';

export const lawyerAdminRoutes: Routes = [
  { path: 'client', component: ClientComponent }
];
